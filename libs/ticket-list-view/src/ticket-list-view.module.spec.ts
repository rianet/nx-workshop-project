import { TicketListViewModule } from './ticket-list-view.module';

describe('TicketListViewModule', () => {
  it('should work', () => {
    expect(new TicketListViewModule()).toBeDefined();
  });
});
