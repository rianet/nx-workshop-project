import { AuthenticationStateModule } from './authentication-state.module';

describe('AuthenticationStateModule', () => {
  it('should work', () => {
    expect(new AuthenticationStateModule()).toBeDefined();
  });
});
